[tool.poetry]
name = "seu-projeto"
version = "0.1.0"
description = ""
authors = ["Seu Nome <leonnardo.campos@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"